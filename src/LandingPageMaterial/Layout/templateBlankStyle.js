import {
    container,
    mlAuto,
    mrAuto,
    title,
    description
  } from "components/material-kit-pro-react/material-kit-pro-react";

const templateBlankStyle = {
    title,
    textCenter: {
        textAlign: "center"
    },
}

export default templateBlankStyle;